package com.github.adrior.roborally.commands;

/**
 * Enum representing the different phases during which commands can be executed.
 */
public enum CommandPhase {
    SETUP_PHASE,
    UPGRADE_PHASE,
    PROGRAMMING_PHASE
}
